

initializing empty git repositoy 

command --> git init

(this command will create the .git folder on your local, now you are ready to do the operations on remote repository through your local repository)

configurations

git config --global user.name "Amit"
git config --global user.email "amitkharse92@gmail.com"

(setting username and the email)

check the current user name and the email lists.

git config --global --list


command to stage/add the files

git add filename
git add .
git add -A


we can commmit the changes by the command -->

git commit -m "message to the commit"


check the current status of the directory. (whether the file is stagged or not? whether the file is commited or not? (basically we can check the status of the files and the folders in the current directory ))

git status

check the logs of the files (like the commiting history)

git log
git log --oneline
git log --pretty=oneline
git log -p -1
